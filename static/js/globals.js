/* Initializes global app object, which will ultimately hold
  the router, models, views, pmpm, etc ... */

window.app = {
  pmpm: {},
  models: {},
  router: {},
  views: {},
  cmp: {} // Current mosaic parameters
};
